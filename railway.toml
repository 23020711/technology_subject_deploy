[railway]
  ignoreFiles = ["frontend/**", "data-processor/**", "crawl-data/**", "*.md", "*.json", "*.toml"]

[build]
  dockerfilePath = "backend/Dockerfile"

[deploy]
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10
